name: 'oms_transforms'
version: '1.0.0'
config-version: 2

profile: 'default'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  oms_transforms:
    +schema: L1_LANDING
    customer_rev:
      customerrevenue:
        +schema: SERVE
        +tags: ['daily']
    emp_reporting:
      emp_weekly_sales:
        +schema: SERVE
        +tags: ['weekly']
